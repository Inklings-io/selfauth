<?php
// prompt for login
// verify login
// redirect back to source

// looks for me, redirect_uri, response_type, state, client_id
                    //'me' => $me,
                    //'redirect_uri' => $redir_url,
                    //'response_type' => 'id',
                    //'state' => substr(md5($trimmed_me . config('app.url') ), 0, 8),
                    //'client_id' => config('app.url')
